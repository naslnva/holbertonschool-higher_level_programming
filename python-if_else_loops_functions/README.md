# 0. Positive or Negative

## Description
This Python script assigns a random signed number to the variable `number` each time it is executed. 
It prints whether the number is **positive**, **negative**, or **zero**.

## Usage
Make sure the script is executable:

```bash
chmod +x 0-positive_or_negative.py
